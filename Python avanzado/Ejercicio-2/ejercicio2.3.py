# Creando una función que muestre la serie fibonaci entre el 0 y el número dado
"""
def fibonacci(num):
    fibonacci_lista = [0]
    a, b = 0, 1
    for i in range(num):
        if b > num:
            return fibonacci_lista
        else:
            fibonacci_lista.append(b)
            a, b = b, a + b


resultado = fibonacci(21)
print(resultado)"""
# Los diccionarios son tipos de lista, como "Clave" "Valor",
# Ponemos la el nombre del valor y luego el valor
